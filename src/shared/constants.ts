// Constants
export const CURRENT_DIR = './';
export const DEFAULT_DST_DIR = 'dst';
export const DEFAULT_SCHEMA_DIR = `${DEFAULT_DST_DIR}/schema`;
export const DEFAULT_THEMES_DIR = `${DEFAULT_DST_DIR}/themes`;
export const DEFAULT_SCHEMA_FILE_NAME = 'dst.schema.json';
export const DEFAULT_THEME = 'default';
export const DEFAULT_SCHEMA_FILE_PATH = `${DEFAULT_SCHEMA_DIR}/${DEFAULT_SCHEMA_FILE_NAME}`;

export const DST_REF_REGEX = /&\[([a-zA-Z0-9.]+)\]/g;
 